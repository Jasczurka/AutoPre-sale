# Руководство по валидации PPTX файлов

## Требования к PPTX файлам

### ✅ Что ДОПУСКАЕТСЯ

1. **Формат файла**
   - PPTX (OpenXML format)
   - PowerPoint 2007 и выше
   - LibreOffice Impress (в формате PPTX)

2. **Структура элементов**
   - Каждый логический элемент = отдельный shape
   - Текстовые блоки с placeholder names
   - Изображения как отдельные shapes
   - Таблицы
   - Графики
   - Фигуры (rectangles, circles, etc.)

3. **Placeholder Names (рекомендуется)**
   ```
   title        - заголовок слайда
   subtitle     - подзаголовок
   body         - основной текст
   image_main   - главное изображение
   image_logo   - логотип
   table_data   - таблица с данными
   chart_stats  - график статистики
   ```

4. **Примеры правильных shapes**
   - Текстовый блок с именем "title"
   - Изображение с именем "image_main"
   - Таблица 3x4 с именем "table_data"
   - Список с маркерами

### ❌ Что НЕ ДОПУСКАЕТСЯ

1. **Старый формат**
   - ❌ PPT (PowerPoint 97-2003)
   - ❌ PPS (PowerPoint Show)
   - ❌ ODP (OpenDocument)

2. **Группировка элементов**
   - ❌ Сгруппированные shapes (Group)
   - ❌ SmartArt как единый объект
   - ❌ Диаграммы с группировкой элементов

3. **Объединение элементов**
   - ❌ Несколько текстовых блоков в одном shape
   - ❌ Текст + изображение в одном shape
   - ❌ Composite shapes

4. **Преобразования**
   - ❌ Текст, преобразованный в изображение
   - ❌ Векторные объекты, преобразованные в растр
   - ❌ Embedded fonts как изображения

## Проверка файла перед импортом

### Автоматическая проверка (через скрипт)

```bash
# Скрипт автоматически проверяет:
# - Расширение файла (.pptx)
# - Существование файла
# - Базовую структуру
python template_manager.py import template.pptx
```

### Ручная проверка в PowerPoint

1. **Откройте файл в PowerPoint**

2. **Проверьте группировку**
   - Выделите все объекты (Ctrl+A)
   - Если видна кнопка "Разгруппировать" - используйте её
   - Разгруппируйте все сгруппированные элементы

3. **Проверьте placeholder names**
   - View → Selection Pane
   - Посмотрите имена всех shapes
   - Переименуйте для удобства (необязательно)

4. **Проверьте типы элементов**
   - Каждый элемент должен быть отдельным
   - Текст должен быть редактируемым (не картинкой)
   - Изображения должны быть отдельными shapes

5. **Сохраните**
   - File → Save As
   - Формат: PowerPoint Presentation (*.pptx)

## Примеры валидации

### Пример 1: Корректный шаблон

```
Слайд 1:
  ├─ Shape 1: TextBox "title" (placeholder)
  ├─ Shape 2: TextBox "subtitle" (placeholder)
  └─ Shape 3: Image "image_logo"

Слайд 2:
  ├─ Shape 1: TextBox "title"
  ├─ Shape 2: TextBox "body"
  └─ Shape 3: Table 4x3 "table_data"

Слайд 3:
  ├─ Shape 1: Rectangle "shape_background"
  └─ Shape 2: TextBox "text_content"
```

✅ **Результат:** Импорт создаст 8 блоков

### Пример 2: Некорректный шаблон (ДО исправления)

```
Слайд 1:
  └─ Group "header_group"
      ├─ TextBox "title"
      └─ Image "logo"

Слайд 2:
  └─ SmartArt "process_diagram"
      ├─ Step 1
      ├─ Step 2
      └─ Step 3
```

❌ **Проблема:** Группировка элементов

### Пример 2: Исправленный шаблон (ПОСЛЕ)

```
Слайд 1:
  ├─ Shape 1: TextBox "title"
  └─ Shape 2: Image "image_logo"

Слайд 2:
  ├─ Shape 1: Rectangle "step1"
  ├─ Shape 2: Rectangle "step2"
  └─ Shape 3: Rectangle "step3"
```

✅ **Результат:** Импорт создаст 5 блоков

## Распространённые ошибки

### Ошибка 1: "Невалидный файл или формат не поддерживается"

**Причина:** Файл не является PPTX

**Решение:**
```bash
# Проверить формат
file template.ppt

# Если .ppt - открыть в PowerPoint и:
# File → Save As → PowerPoint Presentation (.pptx)
```

### Ошибка 2: "Error parsing PPTX"

**Причина:** Сгруппированные элементы или повреждённый файл

**Решение:**
1. Открыть в PowerPoint
2. Выделить все (Ctrl+A)
3. Разгруппировать (Ctrl+Shift+G)
4. Сохранить как новый файл
5. Повторить импорт

### Ошибка 3: "Блоки импортированы, но их меньше ожидаемого"

**Причина:** Некоторые элементы не распознались

**Решение:**
1. Проверить Selection Pane в PowerPoint
2. Убедиться, что все элементы видимы
3. Разгруппировать комплексные объекты
4. Преобразовать SmartArt в shapes (правый клик → Convert to Shapes)

## Checklist перед импортом

```
☐ Файл имеет расширение .pptx
☐ Файл открывается в PowerPoint без ошибок
☐ Все элементы разгруппированы
☐ Нет SmartArt объектов (или преобразованы в shapes)
☐ Текст редактируемый (не картинка)
☐ Каждый логический элемент - отдельный shape
☐ (Опционально) Задаты понятные placeholder names
☐ Файл сохранён в актуальном формате
```

## Тестирование после импорта

```bash
# 1. Импортировать файл
python template_manager.py import template.pptx --version 1.0

# 2. Проверить количество блоков
python template_manager.py list

# 3. Просмотреть детали нескольких блоков
python template_manager.py show <block-id>

# 4. Проверить, что типы блоков корректны
# Ожидаемые типы: text, image, table, chart, shape, placeholder, list
```

## Рекомендации по созданию шаблонов

### 1. Используйте понятные имена

**Плохо:**
```
Shape 1, Shape 2, TextBox 3, Picture 4
```

**Хорошо:**
```
title, subtitle, body, image_main, table_data
```

### 2. Структурируйте по слайдам

**Рекомендация:**
- Слайд 1: Title slide (title, subtitle, logo)
- Слайд 2: Content slide (title, body, image)
- Слайд 3: Data slide (title, table, chart)
- Слайд 4: Closing slide (title, contact_info)

### 3. Используйте placeholders PowerPoint

В PowerPoint:
- Insert → Text Box → затем назначить placeholder type
- Или использовать встроенные placeholders из Layout

### 4. Документируйте шаблон

Создайте README рядом с PPTX:
```markdown
# Corporate Template v1.0

## Слайды:
- Слайд 1: Title (title, subtitle, image_logo)
- Слайд 2: Content (title, body, image_main)
- Слайд 3: Data (title, table_data, chart_stats)

## Placeholder names:
- title: Заголовок слайда (Arial 44pt, bold)
- subtitle: Подзаголовок (Arial 24pt)
- body: Основной текст (Arial 18pt)
...
```

## Утилиты для проверки

### PowerShell скрипт для проверки формата

```powershell
# check_pptx.ps1
param($FilePath)

$extension = [System.IO.Path]::GetExtension($FilePath)
if ($extension -ne ".pptx") {
    Write-Host "❌ Файл не является PPTX" -ForegroundColor Red
    exit 1
}

$zipTest = Test-Path $FilePath
if (-not $zipTest) {
    Write-Host "❌ Файл не найден" -ForegroundColor Red
    exit 1
}

Write-Host "✅ Файл валиден для импорта" -ForegroundColor Green
```

### Python скрипт для анализа структуры

```python
# analyze_pptx.py
from pptx import Presentation
import sys

pptx_file = sys.argv[1]
prs = Presentation(pptx_file)

print(f"📊 Анализ файла: {pptx_file}")
print(f"   Слайдов: {len(prs.slides)}")
print()

total_shapes = 0
for i, slide in enumerate(prs.slides, 1):
    print(f"Слайд {i}: {len(slide.shapes)} shapes")
    for shape in slide.shapes:
        print(f"  - {shape.name} ({shape.shape_type})")
        total_shapes += 1

print()
print(f"Всего shapes: {total_shapes}")
print(f"Ожидаемых блоков: ~{total_shapes}")
```

## Поддержка

Если после всех проверок импорт не работает:

1. Проверьте логи сервиса:
   ```bash
   docker-compose logs template-service
   ```

2. Попробуйте минимальный тестовый файл:
   ```bash
   python test_script.py
   ```

3. Обратитесь к документации:
   - `scripts/README.md`
   - `DEPLOYMENT.md`
   - `ARCHITECTURE.md`
